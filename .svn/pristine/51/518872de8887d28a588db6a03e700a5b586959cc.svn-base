package com.sjsu.client;

 
 
import com.vaadin.Application;
import com.vaadin.ui.*;

/**
 * Main application class.
 */
public class WebsiteGUI extends Application {
 
    @Override
    public void init() {
    	 Window mainWindow = new Window("Msmart Application");
         // setTheme("msmarttheme");
          setTheme("chameleon-green") ;
         
         // MSmart MS = new MSmart();
         // mainWindow.addComponent(MS);
          mainWindow.addComponent(new MSmart());
          mainWindow.setSizeFull();
          setMainWindow(mainWindow);
  		
         /* MS.addListener(new ValueChangeListener() {
              public void valueChange(ValueChangeEvent event) {
                  // Provide some server-side feedback
                  colorname.setValue("Selected color: " + 
                                     colorselector.getColor());
              }
          });*/
          
    }
}

